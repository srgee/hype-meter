[project]
name = "hype-meter"
version = "0.9"
description = "The hype meter app"
readme = "README.md"
requires-python = ">=3.14, <3.15"
dependencies = [
    "django~=6.0.1",
    "django-environ>=0.12.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1,<10",
    "ruff==0.14.14",
]
